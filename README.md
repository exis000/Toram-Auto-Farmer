# Toram-Auto-Farmer
An automated toram farmer for cyclone

""" Run the script then switch to the toram application then its gonna start the script and run the buff first(also checks if 10 mins have passed so it can recast the buff constantly) after then goes into view mode so it can see all mobs clearly and don't change POV after then runs the cyclone(or whatever farmer skill you want to use) N-number of times , REPEAT. (ctrl+c to stop the script) """

#Toram automated farm uses pyautogui, keyboard and time libraries to run the script
#Used for automatic cyclone farmer(bow luck build) but can also be used for other farmers just have to change the hotkey("") and add some buffs and adjust the timer
#The script prints the buff used and at what time it was used for info

Improvements to be made

1) updating an auto gift
2) auto bag discard
3) auto party finder/invite
4) a checker if the user died or you were damaged and the buff is gone
